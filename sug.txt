✅ FIX (VERY IMPORTANT — DO THIS)
🔧 Add similarity threshold

If similarity is weak → return:

Not found in dataset
OR better:

Add keyword filtering BEFORE vector search:

if "gold" not in row['Investing Sectors'].lower():
    skip

👉 This = HYBRID SEARCH (huge upgrade)

🟡 MINOR ISSUE: RELEVANCE PRECISION
Example:
Eikoh Advisory → returned for Gold

But:

It invests in AI, not gold

👉 This means:

semantic similarity is misleading
✅ FIX:

Add post-filtering

Example:

if "gold" not in investing_sectors:
    discard result

👉 This makes system:

🔥 “decision-grade”